#include "poll_poller.h"

using namespace EasyNet;

PollPoller::PollPoller(EventLoop *loop) : Poller(loop) {
    // todo
}

void PollPoller::AddEvent(IOEvent *ev) {
    // todo
}

void PollPoller::DelEvent(IOEvent *ev) {
    // todo
}

void PollPoller::ModEvent(IOEvent *ev) {
    // todo
}

void PollPoller::Polling(int timeout_ms, active_events_t &events) {
    // todo
}